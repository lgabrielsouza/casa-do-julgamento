spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/casa_do_julgamento
    username: postgres
    password: ${DB_PASSWORD}

    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        format_sql: true

    show-sql: false